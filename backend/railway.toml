[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate resolve --applied 20260729000000_init && npx prisma migrate deploy && npm run start:prod"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
